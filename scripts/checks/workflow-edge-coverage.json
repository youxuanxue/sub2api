{
  "_doc": "Gate C registry: per-edge workflow choices stay in sync with deployable matrices, and workflows that mutate the same edge share one concurrency lock.",
  "workflows": {
    ".github/workflows/deploy-edge-stage0.yml": {
      "input": "edge_id",
      "option_prefix": "",
      "required_set": "ec2-deployable",
      "opt_out_edges": []
    },
    ".github/workflows/deploy-edge-lightsail-stage0.yml": {
      "input": "edge_id",
      "option_prefix": "",
      "required_set": "lightsail-deployable",
      "opt_out_edges": []
    },
    ".github/workflows/ops-stage0-pg-dump-refresh.yml": {
      "input": "target",
      "option_prefix": "edge-",
      "required_set": "all-deployable",
      "opt_out_edges": [
        {"id": "us3", "reason": "pg_dump SSM refresh targets the prod EC2 instance only; Lightsail edges are out of scope for this cadence by design"},
        {"id": "us4", "reason": "pg_dump SSM refresh targets the prod EC2 instance only; Lightsail edges are out of scope for this cadence by design"},
        {"id": "us5", "reason": "pg_dump SSM refresh targets the prod EC2 instance only; Lightsail edges are out of scope for this cadence by design"},
        {"id": "us6", "reason": "pg_dump SSM refresh targets the prod EC2 instance only; Lightsail edges are out of scope for this cadence by design"}
      ]
    }
  },
  "concurrency_contracts": [
    {
      "name": "edge-stage0",
      "workflows": [
        ".github/workflows/deploy-edge-stage0.yml",
        ".github/workflows/deploy-edge-lightsail-stage0.yml"
      ],
      "group": "edge-stage0-${{ inputs.edge_id }}",
      "cancel_in_progress": false
    }
  ]
}
