# Local dev: Vite proxies /api, /v1, /setup to this URL.
# ECONNREFUSED = nothing listening here — start Sub2API backend (default port 8080).
# Override in .env.development.local (gitignored) if your API uses another host/port.
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
VITE_DEV_PORT=3000
