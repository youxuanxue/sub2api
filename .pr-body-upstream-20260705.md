## 摘要
- 以 merge commit 合入 upstream/main 最近 5 个提交，保留 upstream 能力与审计链。
- 上游修复：Codex access-only 导入不再错误合并 refresh token；Responses 计费改用 mapped billing model。
- TK VERSION 保持 1.8.84；Invariant 修复 codex import 测试桩缺少 AccountTierService 参数。
- upstream-merge

## 风险
- 热点触达：`openai_gateway_service.go`（Responses 计费 mapped model）、`account_codex_import.go`（Codex 导入去重）。
- 人工决策：VERSION 冲突保留 TK 版本；无 upstream 文件删除；无 OPC 收敛必要（变更量小且为 upstream 原生修复）。

## 验证
- `go test -tags=unit ./internal/handler/admin/... -run Codex`
- `go test -tags=unit ./internal/service/... -run 'RecordUsage|Billing'`
- `./scripts/preflight.sh`（pre-commit 全绿）
- `bash scripts/upstream/check-drift.sh`（合入后应 up-to-date）

## Upstream Audit
- Required audit range: `upstream/main..HEAD`
- TK ahead count: `1360`
- Backend stat top files:
```
 backend/.golangci.yml                              |    32 +-
 backend/Makefile                                   |    18 +-
 backend/cmd/server/VERSION                         |     2 +-
 backend/cmd/server/main.go                         |    76 +-
 backend/cmd/server/wire.go                         |   114 +-
```

## 提交
6bd248fd1 fix(admin): avoid merging Codex access-only imports
4dd3aee5c fix(openai): use mapped billing model for responses
707b87a96 Merge pull request #3676 from wucm667/fix/codex-import-refresh-token-missing-collision
41def4ba0 Merge pull request #3699 from wucm667/fix/responses-billing-use-mapped-model
b650bdd68 chore: sync VERSION to 0.1.144 [skip ci]
6eb4d34a9 merge: upstream/main into TokenKey (5 commits)
51d794848 fix(test): align codex import tests with AccountTierService DI slot
dc7a06fc1 chore: no-web-impact — upstream codex import + responses billing merge
0d8513c27 chore: refresh CI after PR body gate fix (upstream-merge)
- 最新提交：0d8513c27

> Reviewer 请使用 GitHub **Create a merge commit**（不要 Squash and merge）。
