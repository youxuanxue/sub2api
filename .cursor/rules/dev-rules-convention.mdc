---
description: 规则、技能与入口的源头、同步和提交纪律；安装/故障细节按需读取。
alwaysApply: true
---

# dev-rules 约定

## 项目结构

项目使用 `dev-rules` submodule；项目的 submodule SHA 是真相，home mirror 不作为另一个上游。
规则编辑 `dev-rules/rules/`，命令编辑 `dev-rules/commands/`，宪法/hooks/launcher 编辑 `dev-rules/global/`。
`.cursor/rules/*.mdc` 和 `AGENTS.md` 受管块是 sync 产物，禁止手编或把规则改成指向 home mirror 的 symlink。
首次接入、客户端加载位或同步故障，读 `dev-rules/docs/agent-guides/rules-operations.md` 对应章节。

## 新项目接入

缺 submodule 时按上述运维文档接入；使用 `sync.sh` 注册项目、安装 hook/LaunchAgent 并校验，不手工复制全局配置。

## 规则载体分工（防复杂度膨胀）

<!-- rule-carrier-partition-anchor -->

| 载体 | 唯一职责 |
|---|---|
| `global/CLAUDE.md` | 会话硬纪律与触发入口 |
| `rules/product-dev.mdc` | 风险、执行、PR/commit、完成自检 |
| `rules/test-philosophy.mdc` | 测试强度与 Story 升级 |
| `commands/`、skill 入口 | 适用范围、执行路径、关键判断 |
| `docs/agent-guides/`、skill `references/` | 仅相关操作才展开的细则 |
| `README.md`、`AGENTS.md` | 导航，不复制完整规则/技能目录 |
| `digital-clone-research.md §二` | 软规则→硬检查映射 |
| `templates/`、`scripts/` | 确定性检查与操作实现 |
| `.registered-projects`、`.local-projects` | 项目落地状态 |

同一约束只在一个 owner 完整展开；其他入口只留关键边界与读取条件。常驻/技能入口不堆安装手册、修复史或参数百科。暂不能机械化的缺口记 review finding 或 `docs/preflight-debt.md`，不新建流程散文。

## skill / command 确定性基线（机械化优于 prompt 推断）

- 编辑前区分机械步骤与真判断：计数、解析、查表、抓取、校验、排序去重、状态派生由脚本/工具执行；文案/视觉判断、架构权衡、爆炸半径与回滚成本留给模型。
- 复用 `dev-rules/scripts/check_*.py`、preflight 等现有 owner，不让模型重新肉眼判机械事实。反复缺口落成脚本/schema；review 把「本可机械化却写成 prose」列为 finding。
- 项目 skill 只在 `.cursor/skills/` 编辑提交；其它客户端经 `sync.sh` 管理的 symlink 消费，禁止真实副本、手工建链或接管 foreign 条目。description 不超过 Codex 的 1024 字符上限，由 `check_codex_skill_limits.py` 守卫。
- 修改加载位、home registry、hooks/launcher 时先读运维文档；不得触碰 Codex 命令审批规则/内置技能或其它工具自有配置。

## git worktree 与 submodule 约定

操作 worktree 必须用 `git-worktree-submodule`；切换后绑定 `session-workdir` 并跑 `session-check`。禁止手写 submodule 初始化流程或目录 symlink。

## 编辑与提交规则

编辑源头 → `dev-rules/sync.sh --local` → 子模块 preflight/commit/push → 父仓库提交指针与生成产物。父仓库不得指向远端不存在的 SHA。完整命令、跨机器同步见运维文档。

同步前核查 `sync.sh --status`；有落后先 `--pull` / 未推更新先 `--push`。`--pull` 只允许 canonical main，禁止从 feature/detached checkout 全局分发；项目规则仍按各自 submodule SHA 生成。

## 强约束门禁（机械检查，禁止仅“靠自觉”）

父仓库提交前 `sync.sh --check` 必须通过；子模块提交前 `verify-rules.sh` 必须通过。hook/CI 调用项目 preflight，无 wrapper 则 fallback 模板。安装与接线读运维文档，已有门禁不再肉眼重复。无法自动化的段必须记 `docs/preflight-debt.md` 和截止日期；macOS LaunchAgent 不得卸载后不装回。

## approved-doc status 词汇（跨项目共享）

`check_approved_docs.py` 守卫：`draft` 草稿、`pending` 待审批、`approved` 审批基线、`shipped` 已合并、`archived` 不再权威。
`pending` 不得带 related PR/commit；`shipped` 必须带其中之一；`approved_by: pending` 不得落 main/master。项目特异 gate 由 wrapper 管理。

## 禁止事项

禁止编辑 sync 产物、绕过门禁、复制 skill 或擅自替换 foreign owner；具体禁止项在以上各 owner 节。

## Agent 行为约束

缺 submodule 先接入；规则漂移先 `sync.sh --local` 再 `--check`；home drift 按运维文档诊断。所有操作以实际 git/脚本证据为准。
