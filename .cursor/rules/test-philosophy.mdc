---
description: 按风险匹配测试；核心行为验证常驻，完整 Story 细则按需读取。
alwaysApply: true
---

# 测试设计方法论

## 1. 默认路径触发与最小动作

功能变更必须验证真实行为，不默认建立完整 Story 档案。

| 变更 | 最小验证 |
|---|---|
| 新功能 | 至少一个正向和一个负向测试 |
| 行为变更 | 更新/新增对应测试 |
| Bug | 复现 → 修复断言 → 无影响确认 |
| 重构 | 既有测试原样通过，公共 API 表面不变 |
| 删除功能 | 清理对应测试 |

文档/格式/lint、无公共 API 变化的依赖升级、内部重构和聚焦测试可覆盖的小修复不强制建 `.testing/user-stories/`。spec delta 的核心 scenario 须映射测试、手动验证步骤或明确「未验证」。

核心正向 > 核心负向 > 回归 > 输入边界 > 并发时序。自动化测试须能在 CI 运行，环境用 testcontainer/fixture，不依赖手动启动的本地服务/快照。

测试数据从已有 SSOT 的 API、builder、生成器或共享 helper 派生，禁止复制会随 owner 变化的模型/权限/路由整套清单。仅未知 ID、空值、兼容别名等无法派生的固定边界可手写，并标明边界意图。

## 2. 风险升级到完整 Story 路径

风险仅由 `product-dev.mdc` 裁决。新增页面/接口、状态机、安全或基础设施行为变化，只有同时达到高风险才进入完整 Story。此时读 `dev-rules/docs/agent-guides/story-design.md`；其余只补对应核心测试。

## 3. 验证纪律（所有路径必须）

### 运行才算验证

阅读代码不算验证；实际运行并观察行为。绿灯是线索，不能替代真实行为。无法运行写「未验证」；失败附输出，不暗示成功。

### e2e 必须经真实 UI（Playwright）

e2e 必须由 Playwright 真实浏览器走到用户可见 UI。API-only/直调 handler 属单元或集成测试，不能冒充 e2e。有 UI 的端到端验收须实跑浏览器；CLI/库/纯服务不强制 e2e，对外契约风险仍按 Story 细则 §11 展开。项目把此约束固化进 preflight。

### 测试失败归因

失败只证明实现与期望不同；先查是代码 bug 还是测试过期/错误，只修根因。禁止同时改代码和断言对冲失败，禁止为过测删功能。

### Mock 选择

单元可 mock 外部依赖；集成优先真实依赖，mock 隐藏差异时用 testcontainer。

### 验证深度与风险匹配

低风险确认不报错；高风险按 Story 细则 §11 展开逻辑/回归/安全/运行时风险。Bug 与重构按上表。

### 禁止存在性测试

只断言存在/非空/行数、报告已生成或用 grep 代替行为验证，不能作为验收依据。断言返回结果、副作用或运行时行为；`dev-rules/scripts/check_existence_only_tests.py` 为机械门禁。

### 诚实报告

只报告实际运行所得结果与证据。

## 10. 故事推导（测什么）

完整 Story 启用时读 `dev-rules/docs/agent-guides/story-design.md` §10（角色/能力、生命周期、事件、防御需求）。

## 11. 用例设计（四类风险 + 维度矩阵）

高风险或展开无 UI 对外契约时读同一细则 §11。

## 12. 故事落盘规范

创建/更新 Story 时读同一细则 §12 的路径、必填字段和模板。

## 13. Story ↔ Test 对齐

验收完整 Story 时读同一细则 §13；每个 AC 有测试，Linked Tests 可运行，风险有对应断言。
