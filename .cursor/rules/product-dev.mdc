---
description: 研发风险、执行路径、提交与 PR 门禁；默认单 PR 直接实现。
alwaysApply: true
---

# 产品研发工作流

## 默认工作流

单用户意图、单 PR：实现/测试 → preflight → review → 人工确认 → merge → 监控 CI 全绿。
低/常规风险直接做生产级实现，不默认拆 docs/prototype/dev/release PR，不为流程写重复文档。
哲学见 `dev-rules/digital-clone-research.md §一`；测试强度由 `test-philosophy.mdc` 承载。

## 直接执行原则（Plan Mode 校准）

在 harness 允许选择时，先检查必须计划条件；未命中则直接执行。
仅以下情形先计划并等待人工确认：

- 命中下方高风险分类。
- 无法确定用户目标状态，或必须由人裁决业务/架构 trade-off。
- 不可逆外部副作用：生产部署、数据迁移、已 apply schema、大量付费调用。
- 预估 >$5 或 >30min 且失败后不可简单重试。

纯代码/配置尚未 apply、可 `git revert` 的清晰任务直接做。多步骤、多文件、多模块、需要读代码或存在多个合理实现，本身不触发 plan mode。业务判断不猜测；同一问题连续 3 次失败暂停分析并等待人工介入。

## 风险分级（决定走哪条路径）

| 等级 | 判据（高风险优先） | 意图载体 |
|---|---|---|
| 低 | 文档/注释/格式、无公共行为变化的重构、无公共契约变化的依赖升级、聚焦测试可覆盖的小修复 | 任务说明与验证 |
| 常规 | 局部功能/旅程变化，未命中高风险 | PR summary；说不清时才补 spec delta |
| 高 | 下列任一条件 | `docs/approved/` 审批基线 |

高风险条件：

- 公共契约：WebUI/API/CLI/MCP 对外行为、字段语义、状态码或兼容性改变。
- 不可逆状态：DB schema、状态机迁移、审批/计费等高代价状态。
- 安全边界：鉴权、授权、输入安全、租户隔离、凭证/密钥路径。
- 骨架变更：核心架构、基础设施策略、跨系统集成且回滚成本高。
- 核心体验：出错有明显业务损失，普通测试/review 不足以兜底。

文件数、代码量、模块数、评论长度不单独决定风险。拿不准时先按常规补最小澄清；明确安全/数据损失级不确定性才升级。

### 常规风险意图载体

只有 PR summary 不足以表达行为或 reviewer 必须先理解 intent 时，补 `docs/spec-delta-<slug>.md`：Background、Delta（ADDED/MODIFIED/REMOVED）、Scenarios（正/负/回归）、Validation。它不是审批基线，也不能替代测试。

## 高风险路径（例外，而非默认）

命中高风险时，先读 `dev-rules/docs/agent-guides/high-risk-development.md`，按需求 → 原型 → 人工审批 → 实现 → 验证 → 人工合并确认推进。审批前不自行进入下一阶段。原型与生产实现仅在真实决策边界才拆 PR；审批文档从 `approved_by: pending` 起，人的修改/merge 成为符合性基线。

## 分支与提交

- 分支：`feature/[issue-id]-描述`、`fix/[issue-id]-描述`、`chore/描述`、`docs/描述`；高风险原型用 `prototype/`；上游用 `merge/upstream-YYYY-MM-DD`；托管 Agent 的 `cursor/<sessionId>` 可用，squash 时改成业务 commit subject。
- commit 一行解释单个原子变化的原因；PR 只解决一个意图。
- PR body 用中文，默认仅 `摘要`、`风险`、`验证`、`提交`；高风险才展开设计/契约/迁移。技术词、命令、错误码、commit subject 可保留英文。
- `提交` 从 `git log --oneline <base>..HEAD` 生成全部 short SHA + subject，并包含 `最新提交：<HEAD short SHA>`。每次新增 commit 后，push 或继续 review 前重写整个 body，不能只追加备注。
- 有 Web surface 时，后端/业务行为须同 PR 对齐 Web/config/contract/Story；确无影响在 commit message 写 `no-web-impact` 或 `Web impact: none`。
- 高风险改动必须绑定 `docs/approved/*` 或等价审批 anchor；release 语境禁止方括号形式的 skip-ci / ci-skip marker。
- review 默认只报阻塞问题、真实风险、缺失验证。合并后持续监控目标分支/merge queue CI 至全绿；失败、取消、长时间 pending 及时报告并修复或交人工判断。

### Fork 仓库上游治理（Upstream Merge）纪律

对齐外部上游必须加载 `upstream-merge` skill；历史保留、A/B/C commit、冲突审计和收尾由该 skill 单一拥有。项目只补已有不变量 owner、冲突裁决和验证命令。缺 skill 时通过既有安装入口取得，不凭摘要跳过。worktree 用 `git-worktree-submodule`；audit helper 按 skill 目录定位、在宿主 workdir 执行。

## 代码质量门禁

按 `test-philosophy.mdc` 验证真实行为：新功能正/负向、bug 复现、重构既有测试原样通过；禁止存在性测试。

## 长时运行任务专用

Background/Long-running Agent 每完成可验证里程碑立即提交推送，遵守相同 preflight、PR freshness 与审批门禁。

## 完成自检与门禁

汇报须给与风险匹配的已运行验证证据及剩余风险；普通进度不触发全量 preflight。
创建 commit、push、PR 出口、新增 commit 后继续 review、部署、发布、tag、高风险审批或明确要求时，运行 `./scripts/preflight.sh`（无项目 wrapper 用 `dev-rules/templates/preflight.sh`）。失败修复后重跑；禁止带红提交/推送/发布或 `--no-verify` 绕过（紧急回滚除外）。条件门禁不扩成所有任务都跑最高负担。

### 文档数值/事实声明的硬约束

默认不写无助决策的计数（多少段/类/行）；数字本身是设计契约时才用 stat 块并在 `dev-rules/.stats.json` 注册 compute，由 `sync-stats.sh --check` 核对。机制见 `dev-rules/digital-clone-research.md §三`，不包装虚荣计数。

### 人工自检（脚本无法机械化的部分）

核对跨文件语义、approved/规则一致性、重复 owner、示例准确性、commit/PR/设计文档分工。发现问题直接修复，不确定的标入 PR。反复遗漏的检查必须落进 preflight/verify-rules/schema，不能累积口头纪律。

## 云端 Agent / 本地 Agent 运行环境一致性

配置或排障时读 `dev-rules/docs/agent-guides/rules-operations.md` 的云端章节。环境唯一 owner 是 `dev-rules/templates/cloud-agent-bootstrap.sh`；项目只在 `.cursor/cloud-agent.env` 声明工具/secrets。
