# 代码审查报告

> 审查日期：2026-05-07（第三轮，最终确认）
> 审查范围：PR #128 main...feature/pricing-availability-tap（9 commits，HEAD=0c49d101）
> 风险等级：high
> 审查模式：full_conformance
> 审批产物：docs/approved/pricing-availability-source-of-truth.md（approved_by: yyy，2026-05-06）

## 摘要
- 结论：**merge-ready**
- CRITICAL/HIGH: 0 项
- Validation gaps: none

---

## 历轮发现验证（全部已修复）

| 轮次 | Finding | 状态 |
|---|---|---|
| Round 1 R-001 | AntigravityModels 响应 shape 破坏（`[]string` vs `[]ClaudeModel`） | ✅ 已修 — `tkAntigravityDefaultModels()` 始终返回 `[]antigravity.ClaudeModel` |
| Round 1 R-002 | `PricedCandidates()` 返回全平台 catalog，不是 antigravity-only | ✅ 已修 — 候选集改为 `antigravity.DefaultModels()`，zero callers 后 `PricedCandidates()` + `PricedModels()` 已删除 |
| Round 1 CF-001 | GeminiV1BetaListModels 两处 FallbackModelsList 无过滤 | ✅ 已修 — 两处均改为 `tkGeminiFallbackModelsList()` |
| Round 1 R-003 | predicate 无隔离单测 | ✅ 已修 — `pricing_catalog_membership_tk_test.go` + `pricing_availability_predicate_tk_test.go` 均已存在 |
| Round 2 MEDIUM | 5 处 tap 注入无 upstream merge sentinel（§升级原则） | ✅ 已修 — `scripts/pricing-availability-sentinels.json` (6 条目) + `check-pricing-availability-sentinels.py` + preflight.sh 注册，`PASS (6/6)` |
| Round 2 LOW | `PricedCandidates()` / `PricedModels()` 死代码 | ✅ 已修 — 两个方法及其测试均已删除 |

---

## TokenKey 原则最终核查

| 原则 | 结论 | 证据 |
|---|---|---|
| §5 上游 thin 注入 | ✅ | 5 处 tap 各 1 行；Model filter +1 行；AntigravityModels 全由 companion 实现 |
| §5.x 不静默删除 | ✅ | 零 upstream symbol 删除；antigravity/gemini 静态列表保留为 fallback |
| §5.y 历史前向 + §9.2 | ✅ | 9 commits，无 `[skip ci]`；PR body 含 upstream/main..HEAD 审计 |
| §6 接口完整性 | ✅ | PricingCatalogLookup / AvailabilityLookup 各 1 production impl + 1 test stub |
| §8 层依赖 | ✅ | build clean；service 不导入 handler/integration；discover_filter_tk 以 interface 解循环 |
| §9.1 release.yml | ✅ | 不触及 release.yml |
| newapi sentinel 14/14 | ✅ | `python3 scripts/check-newapi-sentinels.py --quiet` PASS |
| pricing-availability sentinel 6/6 | ✅ | `python3 scripts/check-pricing-availability-sentinels.py --quiet` PASS |
| Wire DI 完整性 | ✅ | wire_gen.go:239/256/279/281 — 4 个 TK 注入全部落地 |
| 符合性（vs 审批文档） | ✅（0 偏离）| §2.4 upstream discovery filter、§2.5 unified model-list、§4 tap sites、§5.3 admin API 均与代码一致 |
| 单元测试 | ✅ | 全部 7 个受影响包（cmd/server、handler、handler/admin、handler/dto、service、service/openai_ws_v2、integration/newapi）PASS |
| preflight.sh | ✅ | PASS（含新增 pricing-availability sentinel 检查） |
