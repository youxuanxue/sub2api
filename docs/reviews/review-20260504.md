# 代码审查报告

> 审查日期：2026-05-04
> 审查范围：main...HEAD（PR #110 / merge/upstream-2026-05-04）
> 风险等级：high
> 审查模式：full_conformance
> 审批产物：docs/global/tokenkey-opc-transformation-plan.md、docs/approved/newapi-as-fifth-platform.md、docs/approved/sticky-routing.md、CLAUDE.md

## 摘要

- 结论：merge-ready
- CRITICAL/HIGH: 0 项（本轮发现的阻塞项已修复）
- Validation gaps:
  - Full backend unit suite and frontend checks were not rerun as part of this focused review pass; targeted service tests and `./scripts/preflight.sh` were run after the blocking fixes.
  - QA persistence coverage would still benefit from an end-to-end middleware-to-storage fixture that decompresses captured bytes and asserts secret redaction at rest.

## 已修复发现

### [R-001] backend/internal/service/openai_gateway_chat_completions.go:573 `[DONE]` 被误判为缺失 terminal event

- 原严重程度：critical
- 问题描述：Responses-to-ChatCompletions 流式桥接把只有 `data: [DONE]`、没有 Responses terminal event 的兼容上游流判为错误，可能导致完整响应被计为失败。
- 修复：`[DONE]` 现在走正常 `finalizeStream()`；新增 `TestForwardAsChatCompletions_DoneSentinelWithoutTerminalFinalizesStream`。

### [R-002] backend/internal/service/openai_gateway_chat_completions_raw_test.go:121 raw SSE framing 缺少回归锁定

- 原严重程度：high
- 问题描述：raw chat-completions SSE 透传需要明确防止空行事件分隔被后续改动破坏。
- 修复：新增 `TestForwardAsRawChatCompletions_PreservesSSEBlankLineFraming`，断言 `\n\n` 事件边界仍被透传。

### [R-003] .cursor/skills/tokenkey-upstream-merge/SKILL.md:94 backend Go 命令上下文不准确

- 原严重程度：medium
- 问题描述：skill 中的 Go 命令未说明在 `backend/` module 下运行，容易在 repo root 误执行。
- 修复：改为 `go -C backend generate ...` 和 `go -C backend test ...`。

## 符合性结论

未发现仍阻塞的 TokenKey OPC 偏离：本轮修复后，PR #110 继续保持 TokenKey-only 逻辑向 companion/facade/component 收敛、协议 identity 不为展示词改名、QA/evidence 目标以脱敏后无感记录为边界。
